import Layout from './layout'; // Assuming Layout component is in the same directory
import PageCart from '../pages/PageCart';


const PageCartLayout = () => {
    return (
        <Layout>
            <PageCart />
        </Layout>
    );
};

export default PageCartLayout;